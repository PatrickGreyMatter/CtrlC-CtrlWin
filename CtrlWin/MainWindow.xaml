<Window x:Class="CtrlWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clipboard Manager" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <!-- Height for the splitter -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Clipboard Manager" FontSize="20" FontWeight="Bold" Margin="10"/>

        <ListBox x:Name="ClipboardListBox" Grid.Row="1" Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" SelectionChanged="ClipboardListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Content, Converter={StaticResource TextTrimmerConverter}}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Width="500" VerticalAlignment="Center" Margin="5"/>
                        <Button Content="Copy" Width="75" Height="30" Margin="5" Click="CopyButton_Click" Tag="{Binding Id}"/>
                        <Button Content="Delete" Width="75" Height="30" Margin="5" Click="DeleteButton_Click" Tag="{Binding Id}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" ResizeDirection="Rows" Background="Gray"/>

        <StackPanel Grid.Row="3" Margin="10" Orientation="Vertical" Visibility="Collapsed" x:Name="FullTextPanel">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Height="200">
                <TextBox x:Name="FullTextBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True"/>
            </ScrollViewer>
            <Button Content="Collapse" Width="100" Height="30" Margin="5" Click="CollapseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
